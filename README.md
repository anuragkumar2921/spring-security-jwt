# spring-security-jwt

Spring Security project that implement JWT based authentication and authorization. An endpoint created that can authenticate and return a JWT.  Check incoming requests for JWT in the Authorization header and authorize requests with valid JWT. 